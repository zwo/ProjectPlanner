# 整体需求
实现一个网页，可以包含html, css, javascript文件，要求可以在本地浏览器访问，并包含完整功能。网页的主要功能是可视化的项目规划，用户可以添加任务的开始和结束时间，最后为用户生成项目日历。
# 需求细节
- 网页整体包含3个区域，上方为添加项目任务的控件，中部为展示已经添加的任务的列表，底部为生成项目日历的按钮已经展示生成的项目日历。
- 首次进入只有上方的添加任务的区域可用，中部列表空白，可以给个固定高度，当有多项时可以滚动查看。下方生成项目日历的区域在无任务的时候置灰
- 上部的添加任务区域提供一个"add task"的按钮，旁边有2个radio button，分别是 task 和 milestone。默认选中task
- 点击add task会弹出日历控件，如果是task，需要选择开始日期和结束日期，如果是milestone则只需要选择一个日期。日历控件可参考流行的实现方式，里面也需要提供关闭按钮，取消添加
- 选择完日期后会在中部列表增加一条条目。如果已经存在其它条目，那这个新增的会追加到底部。
- 每个条目都包含一个文本输入框，占位符文字如果是task为"task description"，如果是milestone，则为"milestone description"
- 如果是task，输入框右边放置开始日期和结束日期的文字展示，最右边有个红色的Delete按钮，点击后把条目删除
- 如果是milestone，则只会显示一个日期，其它跟task的一致
- 列表下方是一个"generate calendar"的按钮，可用的时候为蓝色，当列表为空时置灰。
- 点击生成日历按钮会在按钮下方生成以月为单位的日历，如果项目周期横跨多个月份，则展示多个月份，从上到下排列
- 每个月份都需要在顶部显示星期，星期一为最左。当月份不是从星期一开始，则前面的留空。例如7月1日是星期三，那6月29和6月30两天留空
- 每个日期的格子需要显示当天会发生的task或milestone。格子的宽度固定，高度根据内容调整
- 不同的task用不同的颜色展示，颜色随机产生，但是需要用浅色。因为需要用黑色文字显示在上面表示task的描述
- 如果同样的task跨越多天，则色块可以连接在一起，色块可以用圆角矩形，上面的文字靠左
- 如果task跨越多周，则色块和对应的文字都要出现在对应的周
- 如果当日有milestone，则格子里要添加红色五角星旁边带文字说明
- 如果当日有多个task和milestone，则垂直叠加显示，milestone放在最下面
- 日历格子整体效果可以参考 example.jpg